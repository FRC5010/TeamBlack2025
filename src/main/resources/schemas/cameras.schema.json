{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cameras Configuration",
  "description": "List of camera configuration files (matches CameraConfigurationJson.java)",
  "type": "object",
  "required": [
    "cameras"
  ],
  "properties": {
    "cameras": {
      "type": "array",
      "description": "Array of camera configuration file names in the cameras/ subdirectory",
      "items": {
        "type": "string",
        "description": "Camera configuration file name (e.g., 'intake.json')"
      },
      "minItems": 0
    }
  }
}
